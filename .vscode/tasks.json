{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Thong Ton C/C++: g++ build/run/check leak active file.",
            "detail": "You need to open the current file to be build",
            "command": [
                "cd ${fileDirname};",
                "g++ -std=c++11 -g *.h *.cpp -o a.out;",
                "valgrind ./a.out;"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}