// start function createDVDMovie(ifstream & infile)
    // return MovieFactory::createMovie(infile)
// end function

// start function MovieFactory::createMovie(infile)
    // create Movie* movie  = nullptr;
    // making a switch or if-else
        // case D, create new Drama();
        // case C, create new Comedy();
        // case F, create new Classic();
        // default, if not D,C,F in valid command
    // if command exist
        // set data movie, movie->setData(infile)
    //return command 
// end function